@model IEnumerable<UserStatisticsViewModel>

@{
    ViewData["Title"] = "Статистика по пользователям";
}

<div class="container-fluid pb-3">
    <section class="content">

      <!-- Default box -->
      <div class="card card-success">
        <div class="card-header">
          <h3 class="card-title">Пользователи прошедшие</h3>

          <div class="card-tools">
            <button type="button" class="btn btn-tool" data-card-widget="collapse" title="Collapse">
              <i class="fas fa-minus"></i>
            </button>
          </div>
        </div>
        <div class="card-body p-0" style="display: block;">
          <table class="table table-striped projects">
              <thead>

              </thead>
              <tbody>
                  @foreach (var user in Model.Where(s =>s.Status == "прошедший")){
                <tr>
                    <td>
                          @user.UserEmail
                    </td>
                    <td>
                          @user.UserType
                    </td>
                    <td>
                          @user.CompanyName
                    </td>
                    <td>
                        <p class="@user.StatusTextColor">@user.Status</p>
                    </td>
                </tr>
                  }
              </tbody>
          </table>
        </div>
        <!-- /.card-body -->
      </div>
      <!-- /.card -->

    </section>

    <section class="content">

      <!-- Default box -->
      <div class="card card-warning">
        <div class="card-header">
          <h3 class="card-title">Пользователи продолжающие</h3>

          <div class="card-tools">
            <button type="button" class="btn btn-tool" data-card-widget="collapse" title="Collapse">
              <i class="fas fa-minus"></i>
            </button>
          </div>
        </div>
        <div class="card-body p-0" style="display: block;">
          <table class="table table-striped projects">
              <thead>

              </thead>
              <tbody>
                  @foreach (var user in Model.Where(s =>s.Status == "продолжающий")){
                <tr>
                    <td>
                          @user.UserEmail
                    </td>
                    <td>
                          @user.UserType
                    </td>
                    <td>
                          @user.CompanyName
                    </td>
                    <td>
                        <p class="@user.StatusTextColor">@user.Status</p>
                    </td>
                </tr>
                  }
              </tbody>
          </table>
        </div>
        <!-- /.card-body -->
      </div>
      <!-- /.card -->

    </section>

    <section class="content">

      <!-- Default box -->
      <div class="card card-danger">
        <div class="card-header">
          <h3 class="card-title">Пользователи начинающие</h3>

          <div class="card-tools">
            <button type="button" class="btn btn-tool" data-card-widget="collapse" title="Collapse">
              <i class="fas fa-minus"></i>
            </button>
          </div>
        </div>
        <div class="card-body p-0" style="display: block;">
          <table class="table table-striped projects">
              <thead>

              </thead>
              <tbody>
                  @foreach (var user in Model.Where(s =>s.Status == "начинающий")){
                <tr>
                    <td>
                          @user.UserEmail
                    </td>
                    <td>
                          @user.UserType
                    </td>
                    <td>
                          @user.CompanyName
                    </td>
                    <td>
                        <p class="@user.StatusTextColor">@user.Status</p>
                    </td>
                </tr>
                  }
              </tbody>
          </table>
        </div>
        <!-- /.card-body -->
      </div>
      <!-- /.card -->

    </section>
</div>